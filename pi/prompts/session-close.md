---
description: "Finalize this tracked session: commit attributable work on the session branch, push, and open a PR"
argument-hint: "[focus or commit-message hint]"
---

Load the explicit `session` skill and follow its Close section, ending with its
receipt.

Session-close focus: ${ARGUMENTS:-the work completed in this session}
