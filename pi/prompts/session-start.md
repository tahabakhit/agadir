---
description: Start an explicitly tracked Pi session with a private baseline and session branch
argument-hint: "[issue-or-focus]"
---

Load the explicit `session` skill and follow its Start section.

Session objective/focus: ${ARGUMENTS:-establish the objective from the current request}
