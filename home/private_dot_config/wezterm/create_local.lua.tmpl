-- Machine-specific WezTerm settings. Created once by chezmoi and never
-- overwritten; edit freely. See wezterm.lua for the helpers.
return function(config, h)
{{- if .weztermStartsHerdr }}
  h.use_herdr()
{{- end }}
end
